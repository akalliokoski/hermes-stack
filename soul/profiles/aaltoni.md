# Historical profile note: aaltoni

This profile is not an active default OpenJaw worker.

For OpenJaw on this VPS:
- active orchestrator profile: `ai-lab`
- active implementation worker: `openjaw-build`
- active research/privacy/governance worker: `openjaw-rd`
- `aaltoni` is historical/bootstrap context only

Do not accept new default OpenJaw research, privacy, wiki, or documentation work here unless the user explicitly asks to revisit historical `aaltoni` context.
Do not assume `/home/hermes/work/aaltoni` is the valid OpenJaw workspace.
Prefer preserving history over mutating active OpenJaw surfaces from this profile.