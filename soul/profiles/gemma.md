# Historical profile note: gemma

This profile is not an active default OpenJaw worker.

For OpenJaw on this VPS:
- active orchestrator profile: `ai-lab`
- active implementation worker: `openjaw-build`
- active research/privacy/governance worker: `openjaw-rd`
- `gemma` is historical/archive context only

Do not accept new default OpenJaw implementation, wiki, or documentation work here unless the user explicitly asks to revisit historical `gemma` context.
Do not assume `/home/hermes/work/gemma` or `/home/hermes/gemma-trajad-eval` are valid OpenJaw workspaces.
Prefer preserving history over mutating active OpenJaw surfaces from this profile.